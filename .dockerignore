# Root .dockerignore for monorepo docker-compose builds

# Version control
.git
.gitignore

# Editor/IDE
.vscode
.idea
*.suo
*.ntvs_analysis.dat
*.njsproj
*.sln

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Node and package manager artifacts
node_modules
**/node_modules

# Build artifacts and caches (exclude broadly)
dist
build
coverage
.nyc_output
.cache
.next
.turbo
.parcel-cache

# Local env / secrets
.env
.env.*
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker and CI local artifacts
.dockerignore.local
.docker

# Re-include package build outputs that our Dockerfiles expect to COPY
# If your Dockerfiles build these inside the image instead, remove these lines.
!packages/shared/dist/
!packages/frontend/dist/
!packages/backend/dist/

# If you rely on any other generated files in packages, add explicit ! rules for them.
